
%%% Khai báo font
\usepackage{tgtermes} 
% font tương tự Times New Roman
\usepackage{newtxmath} 
% font tương tự Times New Roman cho công thức toán

%%% Khai báo cho gói tabular
\usepackage{multicol}
\usepackage{multirow}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}

%%% Khai báo cho gói cellspace, dùng Sl Sr Sc thay cho l r c nếu dùng tabular
\usepackage[column=S]{cellspace}
\setlength\cellspacetoplimit{5pt}
\setlength\cellspacebottomlimit{5pt}

%%% Khai báo tabularray, khuyên dùng thay cho tabular
\usepackage{tabularray}
\NewColumnType{C}[1]{Q[c, wd=#1]}

%%% Khai báo lệnh tạo hệ phương trình
\newcommand{\hoac}[1]{\left[\begin{aligned}#1\end{aligned}\right.}
\newcommand{\heva}[1]{\left\{\begin{aligned}#1\end{aligned}\right.}

%%% Khai báo thông tin mặc định
\def\truong{THCS XXX}
\def\tobomon{Toán}
\def\giaovien{Phan Văn Anh}
\def\monhoc{Toán}
\def\lophoc{9AX}

%%% Khai báo cho định dạng đoạn
\usepackage{indentfirst}
\setlength{\parindent}{0pt} % Thiết lập độ thụt đầu dòng bằng 0
\setlength{\parskip}{2mm} % Tạo khoảng cách nhỏ giữa các đoạn văn
\AtBeginDocument{
    \sloppy % Giúp xuống dòng tốt hơn 
    \allowdisplaybreaks % Môi trường toán có thể ngắt qua trang nếu quá dài
    \everymath{\displaystyle} % Áp dụng \displaystyle cho mọi môi trường toán
} 
